#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'EOF'
kitty-render-smoke is pending the native Kitty graphics renderer.

The legacy Rust window renderer has been retired. Re-enable this smoke test when
the AppKit/Metal host can render Kitty graphics protocol placements.
EOF
exit 77
